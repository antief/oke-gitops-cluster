apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${GATEWAY_TLS_SECRET_NAME}-apex
  namespace: envoy-gateway-system
spec:
  secretName: ${GATEWAY_TLS_SECRET_NAME}-apex
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - ${BASE_DOMAIN}
